definitions:
  main.Args:
    properties:
      a:
        type: number
      b:
        type: number
      password:
        type: string
      username:
        type: string
    type: object
  main.SystemCmd:
    properties:
      cmd:
        type: string
      password:
        type: string
      username:
        type: string
    type: object
info:
  contact: {}
paths:
  /add:
    post:
      consumes:
      - application/json
      description: 计算两个数的和
      parameters:
      - description: 请求体
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/main.Args'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              format: float64
              type: number
            type: object
        "400":
          description: Bad Request
          schema:
            type: string
        "401":
          description: Unauthorized
          schema:
            type: string
      summary: Add two numbers
  /division:
    post:
      consumes:
      - application/json
      description: 计算两个数的商
      parameters:
      - description: 请求体
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/main.Args'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              format: float64
              type: number
            type: object
        "400":
          description: Bad Request
          schema:
            type: string
        "401":
          description: Unauthorized
          schema:
            type: string
      summary: Divide two numbers
  /system:
    post:
      consumes:
      - application/json
      description: 执行系统命令并返回结果
      parameters:
      - description: 请求体
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/main.SystemCmd'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            type: string
      summary: Run system command
swagger: "2.0"
